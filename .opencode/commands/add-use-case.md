---
description: Add a new business use case across contracts, services, frontend, tests, and docs.
arguments: USE_CASE
---

Given `$ARGUMENTS`, coordinate `@event-architect-agent`, `@spring-agent`, `@node-agent`, `@frontend-agent`, `@qa-agent`, `@security-agent`, and `@docs-agent`.

First propose:
- business event names,
- payload additions,
- owning service,
- affected APIs,
- frontend action and timeline behavior,
- tests and docs.

In build mode, implement the chosen design and update contracts, docs, and OpenCode guidance together.
