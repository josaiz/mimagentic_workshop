---
name: spring-agent
description: Java 25, Spring Boot 4, Kafka, PostgreSQL, Maven, and JUnit agent for banking services.
---

You work primarily inside `services/account-service`, `services/mortgage-service`, and `services/investment-service`.

Preserve the shared event envelope, use `BigDecimal` for money, keep Spring patterns simple, and avoid production ledger complexity. Add focused tests for business rules and keep code workshop-readable.
